<?xml version="1.0" encoding="UTF-8"?>
<schemalist gettext-domain="gdm">
  <enum id="org.gnome.login-screen.BannerSource">
    <value value="1" nick="settings"/>
    <value value="2" nick="file"/>
  </enum>

  <enum id="org.gnome.login-screen.BannerMode">
    <value value="1" nick="auto"/>
    <value value="2" nick="inline"/>
    <value value="3" nick="acknowledgement"/>
  </enum>

  <schema id="org.gnome.login-screen" path="/org/gnome/login-screen/">
    <key name="enable-switchable-authentication" type="b">
      <default>true</default>
      <summary>
        Whether or not to allow switchable authentication for login
      </summary>
      <description>
        The login screen can optionally allow a single PAM service to provide
        multiple authentication mechanisms via a GDM PAM.
      </description>
    </key>
    <key name="enable-fingerprint-authentication" type="b">
      <default>true</default>
      <summary>
        Whether or not to allow fingerprint readers for login
      </summary>
      <description>
        The login screen can optionally allow users who have enrolled
        their fingerprints to log in using those prints.
      </description>
    </key>
    <key name="enable-smartcard-authentication" type="b">
      <default>true</default>
      <summary>
        Whether or not to allow smartcard readers for login
      </summary>
      <description>
        The login screen can optionally allow users who have smartcards
        to log in using those smartcards.
      </description>
    </key>
    <key name="enable-password-authentication" type="b">
      <default>true</default>
      <summary>
        Whether or not to allow passwords for login
      </summary>
      <description>
        The login screen can be configured to disallow password authentication,
        forcing the user to use smartcard or fingerprint authentication.
      </description>
    </key>
    <key name="logo" type="s">
      <default>''</default>
      <summary>
        Path to small image at top of user list
      </summary>
      <description>
        The login screen can optionally show a small image to provide site
        administrators and distributions a way to display branding.
      </description>
    </key>
    <key name="disable-user-list" type="b">
      <default>false</default>
      <summary>
        Avoid showing user list
      </summary>
      <description>
        The login screen normally shows a list of available users to log in
        as. This setting can be toggled to disable showing the user list.
      </description>
    </key>
    <key name="banner-message-enable" type="b">
      <default>false</default>
      <summary>
        Enable showing the banner message
      </summary>
      <description>
        Set to true to show the banner message text.
      </description>
    </key>
    <key name="banner-message-mode" enum="org.gnome.login-screen.BannerMode">
      <default>"auto"</default>
      <summary>
        Banner message presentation mode
      </summary>
      <description>
        How the banner message is presented on the login screen. "inline"
        shows it as a small message above the login prompt. "acknowledgement"
        shows it after the user authenticates and requires the user to
        acknowledge it before the session starts. "auto" automatically
        determines "inline" or "acknowledgement" based on length of the
        banner's content.
      </description>
    </key>
    <key name="banner-message-source" enum="org.gnome.login-screen.BannerSource">
      <default>"settings"</default>
      <summary>
        Banner message source
      </summary>
      <description>
        The source of the text banner message on the login screen.
      </description>
    </key>
    <key name="banner-message-title" type="s">
      <default>''</default>
      <summary>
        Banner message title
      </summary>
      <description>
        Title of the banner message to show in the login window.
      </description>
    </key>
    <key name="banner-message-text" type="s">
      <default>''</default>
      <summary>
        Banner message text
      </summary>
      <description>
        Text banner message to show in the login window.
      </description>
    </key>
    <key name="banner-message-path" type="s">
      <default>''</default>
      <summary>
        Banner message path
      </summary>
      <description>
        Path to text file with banner message to show in the login window.
      </description>
    </key>
    <key name="banner-message-button" type="s">
      <default>''</default>
      <summary>
        Banner message acknowledgement button label
      </summary>
      <description>
        Label for the acknowledgement button shown with the banner message
        when banner-message-mode presents it as something the user must
        acknowledge. If empty, a default label is used.
      </description>
    </key>
    <key name="disable-restart-buttons" type="b">
      <default>false</default>
      <summary>
        Disable showing the restart buttons
      </summary>
      <description>
        Set to true to disable showing the restart buttons in the login window.
      </description>
    </key>
    <key name="allowed-failures" type="i">
      <default>3</default>
      <summary>
        Number of allowed authentication failures
      </summary>
      <description>
        The number of times a user is allowed to attempt authentication, before
        giving up and going back to user selection.
      </description>
    </key>
  </schema>
</schemalist>
